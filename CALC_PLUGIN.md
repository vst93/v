# Calc Plugin - Advanced Calculator v1.0.0

## 概述

`calc` 是一个功能完整的高级计算器插件，支持命令行模式和交互式 TUI 模式。

---

## ✨ 功能特性

### 1. 双模式支持

**命令行模式：**
```bash
v calc -e '2+2'
# 输出: 2+2 = 4
```

**交互式 TUI 模式：**
```bash
v calc
# 启动交互式计算器界面
```

### 2. 支持的运算

**基础运算符：**
- `+` 加法
- `-` 减法
- `*` 乘法
- `/` 除法
- `%` 取模
- `^` 幂运算

**数学函数：**
- `sin(x)` - 正弦
- `cos(x)` - 余弦
- `tan(x)` - 正切
- `sqrt(x)` - 平方根
- `abs(x)` - 绝对值
- `log(x)` - 常用对数（底数10）
- `ln(x)` - 自然对数（底数e）
- `exp(x)` - e^x
- `floor(x)` - 向下取整
- `ceil(x)` - 向上取整
- `round(x)` - 四舍五入

**数学常量：**
- `pi` - 圆周率 (3.14159...)
- `e` - 自然常数 (2.71828...)

**分组：**
- `( )` - 括号用于改变运算优先级

---

## 📖 使用方法

### 命令行模式

```bash
# 基础运算
v calc -e '2 + 2'
# 输出: 2 + 2 = 4

# 高级表达式
v calc -e 'sqrt(16) + 2^3'
# 输出: sqrt(16) + 2^3 = 12

# 三角函数
v calc -e 'sin(pi/2)'
# 输出: sin(pi/2) = 1

# 复杂表达式
v calc -e '(10 + 5) * 2 / 3'
# 输出: (10 + 5) * 2 / 3 = 10

# 对数运算
v calc -e 'log(100) + ln(e^2)'
# 输出: log(100) + ln(e^2) = 4
```

### 交互式 TUI 模式

```bash
v calc
```

**TUI 功能特点：**
- 实时输入表达式
- 即时显示计算结果
- 保留最近 5 条历史记录
- 错误提示显示
- 优雅的界面设计

**键盘控制：**
- `Enter` - 计算表达式
- `Ctrl+L` - 清除输入
- `Ctrl+C` - 退出计算器

---

## 🎯 使用示例

### 场景 1: 快速计算

```bash
# 计算折扣价格
v calc -e '299 * 0.8'
# 输出: 299 * 0.8 = 239.2

# 计算百分比
v calc -e '(45 / 180) * 100'
# 输出: (45 / 180) * 100 = 25
```

### 场景 2: 科学计算

```bash
# 圆的面积
v calc -e 'pi * 5^2'
# 输出: pi * 5^2 = 78.5398

# 指数增长
v calc -e '1000 * e^0.05'
# 输出: 1000 * e^0.05 = 1051.27
```

### 场景 3: 工程计算

```bash
# 三角计算
v calc -e 'sqrt(3^2 + 4^2)'
# 输出: sqrt(3^2 + 4^2) = 5

# 对数计算
v calc -e 'log(1000)'
# 输出: log(1000) = 3
```

### 场景 4: 交互式探索

```bash
v calc
# 进入 TUI 模式，可以连续输入多个表达式
# 每次计算后保留历史记录
# 适合需要多次计算的场景
```

---

## 🔧 技术实现

### 表达式解析器

采用递归下降解析器（Recursive Descent Parser）实现：

1. **运算符优先级（从高到低）：**
   - 括号 `( )`
   - 函数调用 `sin()`, `sqrt()` 等
   - 幂运算 `^` （右结合）
   - 乘除模 `*`, `/`, `%`
   - 加减 `+`, `-`

2. **解析层级：**
   ```
   parseExpression()  → 处理 + -
   parseTerm()        → 处理 * / %
   parsePower()       → 处理 ^
   parseFactor()      → 处理数字、函数、括号、正负号
   parseNumber()      → 解析数字
   parseFunction()    → 解析函数调用
   ```

3. **错误处理：**
   - 除零检查
   - 非法函数参数（如负数开方、非正数对数）
   - 括号不匹配检测
   - 未知函数识别

### TUI 界面

使用 Bubbletea 框架实现：
- 文本输入组件（textinput）
- 实时结果显示
- 历史记录管理
- Lipgloss 样式美化

---

## 📋 运算符优先级说明

```
优先级从高到低：
1. ( )           括号
2. 函数           sin(), sqrt() 等
3. ^             幂运算（右结合，2^3^2 = 2^(3^2) = 512）
4. *, /, %       乘法、除法、取模
5. +, -          加法、减法
```

**示例：**
```bash
v calc -e '2 + 3 * 4'        # = 14 (先乘后加)
v calc -e '(2 + 3) * 4'      # = 20 (括号优先)
v calc -e '2^3^2'            # = 512 (右结合：2^(3^2))
v calc -e 'sin(pi/4) * 2'    # = 1.41421 (函数先计算)
```

---

## ⚠️ 注意事项

1. **三角函数使用弧度制**
   ```bash
   v calc -e 'sin(90)'     # ≈ 0.894 (错误：当作弧度)
   v calc -e 'sin(pi/2)'   # = 1 (正确：π/2 弧度 = 90°)
   ```

2. **常量替换**
   - `pi` 和 `e` 会被替换为其数值
   - 替换发生在解析之前

3. **浮点数精度**
   - 内部使用 float64
   - 整数结果显示为整数
   - 小数结果最多显示 6 位有效数字

4. **错误提示**
   - 命令行模式：错误信息直接输出
   - TUI 模式：红色高亮显示错误

---

## 🎨 TUI 界面预览

```
╭─────────────────────────────────────────────────────────────╮
│                                                               │
│  🧮 Advanced Calculator                                       │
│                                                               │
│  Expression: sqrt(16) + 2^3█                                  │
│                                                               │
│  Result: 12                                                   │
│                                                               │
│  History:                                                     │
│    1. sqrt(16) + 2^3 = 12                                    │
│    2. sin(pi/2) = 1                                          │
│    3. 2+2 = 4                                                │
│                                                               │
│  Enter: evaluate | Ctrl+L: clear | Ctrl+C: quit | Supports:  │
│  +,-,*,/,^,sqrt,sin,cos,tan,log,ln,pi,e                      │
│                                                               │
╰─────────────────────────────────────────────────────────────╯
```

---

## 📊 功能对比

| 特性 | calc 插件 | 系统计算器 |
|------|----------|-----------|
| 命令行调用 | ✅ | ❌ |
| 交互式界面 | ✅ | ✅ |
| 高级函数 | ✅ 12种 | ✅ |
| 数学常量 | ✅ pi, e | ✅ |
| 历史记录 | ✅ 最近5条 | ✅ |
| 表达式输入 | ✅ 字符串 | ❌ 按钮 |
| 脚本集成 | ✅ | ❌ |

---

## 🚀 快捷命令

```bash
# 使用别名
v calculator -e '2+2'

# 查看帮助
v calc -h
```

---

## 📝 版本信息

- **版本：** 1.0.0
- **作者：** vst
- **别名：** calculator

---

## ✅ 测试验证

所有功能已通过测试：

```bash
✅ 基础运算: 2+2 = 4
✅ 高级表达式: sqrt(16) + 2^3 = 12
✅ 三角函数: sin(pi/2) = 1
✅ 括号优先级: (10 + 5) * 2 / 3 = 10
✅ 对数运算: log(100) + ln(e^2) = 4
✅ 别名调用: calculator 正常工作
✅ TUI 模式: 交互界面正常运行
```

---

## 🎉 总结

`calc` 插件提供了完整的计算器功能：

✅ 支持命令行和 TUI 双模式  
✅ 丰富的数学函数和运算符  
✅ 正确的运算符优先级  
✅ 友好的错误提示  
✅ 历史记录管理  
✅ 优雅的界面设计
