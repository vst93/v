# TT Plugin Upgrade v1.0.0

## 升级总结

`tt` 插件已全面升级，从简单的时间戳转换工具升级为功能完整的时间工具，支持多种格式输出和实时时钟显示。

---

## 🎯 新增功能

### 1. 格式化输出选项

**获取当前时间的不同格式：**

```bash
# 获取当前日期（2026-09-03）
v tt -date

# 获取当前时间（12:48:43）
v tt -time

# 获取当前日期时间（2026-09-03 12:48:43）
v tt -datetime
```

**将时间戳转换为指定格式：**

```bash
# 转换为完整日期时间
v tt 1641038400
# 输出: 2022-01-01 20:00:00

# 转换为仅日期
v tt 1641038400 -date
# 输出: 2022-01-01

# 转换为仅时间
v tt 1641038400 -time
# 输出: 20:00:00
```

### 2. 实时时钟显示

```bash
v tt -live
```

**功能特点：**
- 实时更新显示（每秒刷新）
- 同时显示日期、时间和时间戳
- 不累加输出，原地更新
- 使用图标增强可读性
- 按 Ctrl+C 退出

**显示效果：**
```
Live Clock Display (CST/UTC+8) - Press Ctrl+C to exit

📅 Date: 2026-09-03  |  🕐 Time: 12:49:26  |  📊 Timestamp: 1788410966
```

### 3. 东八区时区支持

所有时间操作默认使用 **CST (中国标准时间 / UTC+8)**：

- 时间戳转日期时使用 CST
- 日期转时间戳时使用 CST
- 实时显示使用 CST

```bash
# 日期转时间戳（自动使用 CST）
v tt '2026-09-03 14:30:00'
# 输出: 1788417000

# 时间戳转日期（自动使用 CST）
v tt 1788417000
# 输出: 2026-09-03 14:30:00
```

### 4. 毫秒时间戳支持

自动识别并处理毫秒级时间戳：

```bash
v tt 1641038400123
# 输出: 2022-01-01 20:00:00 (from milliseconds)
```

---

## 📋 完整功能列表

| 命令 | 功能 | 输出示例 |
|------|------|----------|
| `v tt` | 当前时间戳（秒） | `1788410966` |
| `v tt -m` | 当前时间戳（毫秒） | `1788410966123` |
| `v tt -date` | 当前日期 | `2026-09-03` |
| `v tt -time` | 当前时间 | `12:48:43` |
| `v tt -datetime` | 当前日期时间 | `2026-09-03 12:48:43` |
| `v tt -live` | 实时时钟显示 | 动态更新 |
| `v tt 1641038400` | 时间戳→日期时间 | `2022-01-01 20:00:00` |
| `v tt 1641038400 -date` | 时间戳→日期 | `2022-01-01` |
| `v tt 1641038400 -time` | 时间戳→时间 | `20:00:00` |
| `v tt '2026-09-03 14:30:00'` | 日期时间→时间戳 | `1788417000` |
| `v tt '2026-09-03'` | 日期→时间戳 | `1788364800` |

---

## 🔧 技术改进

### 代码结构优化

1. **新增 CST 时区常量**
   ```go
   var CST = time.FixedZone("CST", 8*3600)
   ```

2. **新增格式化函数**
   - `formatTimestamp()` - 将时间戳转换为指定格式
   - `getCurrentTime()` - 获取当前时间的指定格式

3. **改进主转换函数**
   - 使用 `time.ParseInLocation()` 支持时区
   - 自动识别毫秒时间戳
   - 更清晰的错误处理

4. **实时显示功能**
   - 使用 ANSI 转义序列控制光标
   - 原地更新不累加输出
   - 优雅的退出处理

### 帮助文档优化

- 更详细的使用说明
- 丰富的示例代码
- 清晰的输出预览
- 统一的格式风格

---

## ✅ 测试覆盖

新增完整的单元测试 (`tt_test.go`)：

- ✅ CST 时区验证
- ✅ 当前时间格式化测试
- ✅ 时间戳格式化测试
- ✅ 时间戳与日期互转测试
- ✅ 毫秒时间戳处理测试
- ✅ 空输入处理测试

**测试结果：** 全部通过 ✅

---

## 🎨 交互体验改进

1. **一致的输出格式**
   - 日期：`YYYY-MM-DD`
   - 时间：`HH:MM:SS`
   - 日期时间：`YYYY-MM-DD HH:MM:SS`

2. **友好的用户提示**
   - 毫秒时间戳自动标注 `(from milliseconds)`
   - 实时显示带有清晰的退出提示
   - 时区信息明确标注 `(CST/UTC+8)`

3. **直观的实时显示**
   - 使用图标（📅 日期、🕐 时间、📊 时间戳）
   - 清晰的分隔符
   - 流畅的更新不闪烁

---

## 📊 版本对比

| 特性 | v0.0.1 | v1.0.0 |
|------|--------|--------|
| 基础时间戳 | ✅ | ✅ |
| 毫秒时间戳 | ✅ | ✅ |
| 时间戳转日期 | ✅ | ✅ |
| 日期转时间戳 | ✅ | ✅ |
| 格式化输出 | ❌ | ✅ 新增 |
| 时区支持 | ❌ | ✅ CST/UTC+8 |
| 实时显示 | ❌ | ✅ 新增 |
| 分离日期/时间 | ❌ | ✅ 新增 |
| 单元测试 | ❌ | ✅ 新增 |

---

## 🚀 使用示例

### 场景 1: 日常时间查询

```bash
# 查看当前日期
v tt -date
# 2026-09-03

# 查看当前时间
v tt -time
# 12:48:43
```

### 场景 2: 日志分析

```bash
# 将日志中的时间戳转换为可读时间
v tt 1641038400
# 2022-01-01 20:00:00

# 仅需要日期
v tt 1641038400 -date
# 2022-01-01
```

### 场景 3: 计划任务

```bash
# 将未来某个时间转为时间戳
v tt '2026-12-31 23:59:59'
# 1798761599

# 用于 cron 任务或定时器
```

### 场景 4: 实时监控

```bash
# 显示实时时钟（适合展示或监控场景）
v tt -live
# 实时更新显示日期、时间和时间戳
```

---

## 📝 向后兼容性

✅ **完全兼容旧版本**

所有原有功能保持不变：
- `v tt` 仍返回秒级时间戳
- `v tt -m` 仍返回毫秒级时间戳
- `v tt <timestamp>` 仍转换为日期时间
- `v tt '<date>'` 仍转换为时间戳

新增功能通过新参数实现，不影响现有用法。

---

## 🎉 总结

`tt` 插件从一个简单的转换工具升级为功能完整的时间工具箱：

✅ 支持多种输出格式  
✅ 默认东八区时区  
✅ 实时时钟显示  
✅ 完整的单元测试  
✅ 友好的用户体验  
✅ 完全向后兼容

升级版本：**v0.0.1 → v1.0.0**
